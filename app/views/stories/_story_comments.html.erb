<ul class="list-group top top--collapse">
  <% comments['top'].each do |comment| %>
    <li class="list-group-item d-flex justify-content-start align-items-center">
        <%= render partial: "comment", locals: { comment: comment } %>
    </li>
  <% end %>
</ul>
